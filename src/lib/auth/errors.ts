export const DUPLICATE_EMAIL_MESSAGE = "An account with this email already exists.";

/**
 * Deliberately identical for an unknown email and a wrong password, so the login
 * form cannot be used to discover which addresses are registered.
 */
export const INVALID_CREDENTIALS_MESSAGE = "Incorrect email or password.";
